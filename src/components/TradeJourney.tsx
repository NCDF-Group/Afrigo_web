'use client'
import type { Role } from '@/lib/roles'
const stages=[['1','Buyer requests','Buyer publishes an RFQ or reserves Seller inventory.'],['2','Seller commits','Seller quotes from a specific inventory lot and accepts the awarded contract.'],['3','Buyer pays','Paystack confirms payment before logistics can begin.'],['4','Exporter takes custody','Buyer selects a verified Exporter. A shipment is created and the Exporter coordinates pickup, documents, customs, carrier handoff, and delivery.'],['5','Settlement','Delivery completes the contract. Seller requests payout and operations approves the reserved balance.']]
export default function TradeJourney({role}:{role:Role}){const active=role==='Buyer'?0:role==='Seller'?1:3;return <section className="mt-6 rounded-3xl border bg-slate-950 p-5 text-white sm:p-7"><p className="text-xs font-bold uppercase tracking-[.2em] text-amber-300">How this trade moves</p><h3 className="mt-2 text-xl font-black">{role==='Exporter'?'Your role starts after payment':'Buyer → Seller → Exporter handoff'}</h3><div className="mt-5 grid gap-3 lg:grid-cols-5">{stages.map(([number,title,detail],index)=><div key={number} className={`rounded-2xl border p-4 transition hover:-translate-y-1 ${index===active?'border-amber-300 bg-white/10':'border-white/10 bg-white/5'}`}><span className="inline-flex h-7 w-7 items-center justify-center rounded-full bg-amber-300 text-xs font-black text-slate-950">{number}</span><p className="mt-3 font-bold">{title}</p><p className="mt-1 text-xs leading-5 text-white/65">{detail}</p></div>)}</div>{role==='Exporter'&&<p className="mt-4 rounded-2xl bg-emerald-400/10 p-4 text-sm text-emerald-100">You do not quote or sell the goods. You enter only after a paid contract is assigned to your verified Exporter account, then own the physical and compliance journey until confirmed delivery.</p>}</section>}
